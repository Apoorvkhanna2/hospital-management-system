pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo "✅ Simple test successful!"
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}